<%= form_for(@item) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :description, placeholder: "Description" %>
  </div>
  <div class="field">
    <%= f.label :price, "Price (€)" %>
    <%= f.number_field :price, min: 0 %>
  </div>
  <div class="field">
    <%= f.date_select :date_bought %>
  </div>
  <%= f.submit "Add", class: "btn btn-large btn-primary" %>
<% end %>
